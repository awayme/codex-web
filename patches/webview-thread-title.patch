--- a/webview/assets/local-conversation-thread-BaIREoHa.js
+++ b/webview/assets/local-conversation-thread-BaIREoHa.js
@@ -21131,8 +21131,16 @@
     L = (0, Sk.useContext)(Ck),
     R = X(bc, n) ?? !0,
     z = X(nn, n),
+    codexHostedWindowTitle = X(xt, {
+      conversationId: n,
+      hostId: l,
+    })?.title,
     B = _r(`2138468235`).get(`enable_mcp_apps`, !1) && gt();
   (X(`standard`, n), X(!1, n));
+  (0, Sk.useEffect)(() => {
+    let e = codexHostedWindowTitle?.trim();
+    e && (document.title = `${e} | Codex`);
+  }, [codexHostedWindowTitle]);
   let V = lu(`1579719221`) && !0,
     H = lu(`188145323`),
     U;
