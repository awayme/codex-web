--- a/webview/assets/app-initial-BTphDPeq.js
+++ b/webview/assets/app-initial-BTphDPeq.js
@@ -363245,13 +363245,29 @@
               restoreRichLinks: !C,
             })
           : _$r({ schema: y, text: e }));
-  let A = new rEn(null, {
-      attributes: {
-        "aria-multiline": `true`,
-        dir: `auto`,
-        role: `textbox`,
-        spellcheck: `true`,
-      },
+  let inputModeDisabled = !1,
+    getAttributes = () =>
+      inputModeDisabled
+        ? {
+            "aria-multiline": `true`,
+            dir: `auto`,
+            role: `textbox`,
+            spellcheck: `true`,
+          }
+        : {
+            "aria-multiline": `true`,
+            dir: `auto`,
+            role: `textbox`,
+            spellcheck: `true`,
+            inputmode: `none`,
+          },
+    setPointerInputMode = (e) => {
+      inputModeDisabled !== e &&
+        ((inputModeDisabled = e),
+        A.isDestroyed || A.setProps({ attributes: getAttributes() }));
+    },
+    A = new rEn(null, {
+      attributes: getAttributes(),
       transformPastedHTML(e) {
         return e.length > NGa ? ` ` : nMn(mTa(e).body.innerHTML) || ` `;
       },
@@ -363361,6 +363377,15 @@
           b.dispatchEvent(new CustomEvent(NUa, { detail: e.docChanged })));
       },
       handleDOMEvents: {
+        mousedown() {
+          return (setPointerInputMode(!0), !1);
+        },
+        touchstart() {
+          return (setPointerInputMode(!0), !1);
+        },
+        blur() {
+          return (setPointerInputMode(!1), !1);
+        },
         keyup(e, t) {
           let { $from: n, empty: r } = e.state.selection;
           return (
