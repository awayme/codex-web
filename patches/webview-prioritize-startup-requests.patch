--- a/webview/assets/app-initial-BTphDPeq.js
+++ b/webview/assets/app-initial-BTphDPeq.js
@@ -69809,7 +69809,7 @@
             s = await this.listRecentThreads({
               limit: i ? Math.min(a, 100) : a,
               cursor: null,
-              background: i,
+              background: !1,
             });
           if (s.data.length < a && s.nextCursor != null) {
             let e = [...s.data],
@@ -69913,7 +69913,7 @@
               r,
               n
                 ? { priority: `background`, source: `recent_threads` }
-                : { source: `recent_threads` },
+                : { priority: `critical`, source: `recent_threads` },
             );
           return { ...i, data: i.data.filter(sat) };
         }
@@ -116878,7 +116878,7 @@
       ]));
   });
 function NQt() {
-  return kQt() ? { priority: `critical` } : {};
+  return { priority: `critical` };
 }
 var PQt = n(() => {
     sb();
@@ -359706,11 +359706,15 @@
             queryFn: () => (
               i && l.removeQueries({ exact: !0, queryKey: yVa(n, t, a) }),
-              Lg(u, n).sendRequest(`model/list`, {
-                includeHidden: !0,
-                cursor: null,
-                limit: a,
-              })
+              Lg(u, n).sendRequest(
+                `model/list`,
+                {
+                  includeHidden: !0,
+                  cursor: null,
+                  limit: a,
+                },
+                { priority: `critical` },
+              )
             ),
             select: ({ data: n }) =>
               cVa({
