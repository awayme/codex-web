--- a/webview/assets/app-initial-BTphDPeq.js
+++ b/webview/assets/app-initial-BTphDPeq.js
@@ -658501,11 +658501,24 @@
             preserveUnresolvedSkillMentions: !0,
           }).content
         : re.get(XV),
+    hostedPromptSearchParam = new URLSearchParams(window.location.search).get(
+      `prompt`,
+    ),
+    hostedDefaultText =
+      be === `` ? (hostedPromptSearchParam ?? be) : be,
     xe;
   t[12] === re ? (xe = t[13]) : ((xe = yV(re)), (t[12] = re), (t[13] = xe));
   let Se = xe,
     Ce = BFr(Se),
     we = gs(Fk, Se),
+    hostedComposerController = O;
+  (0, wrl.useEffect)(() => {
+    hostedPromptSearchParam != null &&
+      hostedPromptSearchParam !== `` &&
+      hostedComposerController.getText() === `` &&
+      hostedComposerController.setPromptText(hostedPromptSearchParam);
+  }, [hostedComposerController, hostedPromptSearchParam]);
+  let
     Te = gs(RO, we),
     Ee = gs(sCr, we),
     De =
